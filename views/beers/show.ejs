
<!DOCTYPE html>
<html>
  <head>
    <!-- Make site responsive on mobile/tablet -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>BRUNO - Beers</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <script defer src="https://use.fontawesome.com/releases/v5.2.0/js/all.js" integrity="sha384-4oV5EgaV02iISL2ban6c/RmotsABqE4yZxZLcYMAdG7FAPsyHYAPpywE9PJo+Khy" crossorigin="anonymous"></script>
    
    <link rel="stylesheet" href="/css/main.css" type="text/css"/>
  </head>
  <body class="mb-5">
      <nav class="navbar navbar-expand-md navbar-dark bg-dark mb-4">
        <a class="navbar-brand" href="/">BRUNO</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item">
              <a class="nav-link" href="/beers">Beers</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/recipes">Recipes</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/brews">Brews</a>
            </li>
          </ul>
          <ul class="navbar-nav navbar-right">
            <li class="nav-item">
              <a class="nav-link" href="#">Future Login Link</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Future Login Link</a>
            </li>
          </ul>
        </div>
      </nav>
      <div class="container">
        <div class="row justify-content-center mb-2">
          <% for(let beer of beers) { %>
            <div class="col-12 mb-2 px-2">
              <div class="card">
                <div class="card-body">
                  <h3 class="d-inline-block mb-0"><%= beer.name %></h3>
                  <span class="pl-1"><%= beer.style %></span>
                  <p class="text-lite"><%= beer.description %></p>
                  <button type="button" class="btn btn-outline-secondary" data-toggle="collapse" data-target="#recipes<%= beer.id %>" aria-expanded="false" aria-controls="recipes<%= beer.id %>">Recipes</button>
                  <button type="button" class="btn btn-outline-secondary" data-toggle="collapse" data-target="#brews<%= beer.id %>" aria-expanded="false" aria-controls="brews<%= beer.id %>">Brews</button>
                </div>
                <!-- Collapsing list of recipes -->
                <div id="recipes<%= beer.id %>" class="collapse">
                  <div class="card-body pt-0">
                    <% if (beer.recipes.length > 0) { %>
                    <table class="text-lite table table-sm">
                      <thead>
                        <tr>
                          <th class="recipe-label text-center" scope="col">Version</th>
                          <th class="recipe-label text-center" scope="col">Date Added</th>
                          <th class="recipe-label text-center" scope="col">Last Brewed</th>
                          <th class="recipe-label text-center" scope="col">Note</th>
                          <th class="recipe-label text-center" scope="col"></th>
                        </tr>
                      </thead>
                      <tbody>
                        <% for (let recipe of beer.recipes) { %>
                          <tr class="text-center">
                            <td><%= recipe.version %></td>
                            <td><%= recipe.dateAdded %></td>
                            <td><%= recipe.lastBrewed %></td>
                            <td><%= recipe.note %></td>
                            <td><a href="beers/<%= beer.id %>/recipes/<%= recipe.id %>" class="btn btn-sm btn-outline-dark mb-2 float-right" role="button"><i class="far fa-eye"></i></a></td>
                          </tr>
                        <% } %>          
                      </tbody>
                    </table>
                    <% } else { %>
                    <p class="text-lite">This beer doesn't have any recipes yet.</p>
                    <% } %>
                    <div class="d-flex flex-row-reverse">
                      <a class="btn btn-outline-success btn-sm mb-1" role="button">New Recipe</a>
                    </div>
                  </div>
                </div>
                <!-- Collapsing List of Brews -->
                <div id="brews<%= beer.id %>" class="collapse">
                  <div class="card-body pt-0">
                    <% if (beer.brews.length > 0) { %>
                    <table class="text-lite table table-sm">
                      <thead>
                        <tr>
                          <th class="recipe-label text-center" scope="col">Brew Date</th>
                          <th class="recipe-label text-center" scope="col">Recipe Version</th>
                          <th class="recipe-label text-center" scope="col"></th>
                          <th class="recipe-label text-center" scope="col"></th>
                          <th class="recipe-label text-center" scope="col"></th>
                        </tr>
                      </thead>
                      <tbody>
                        <% for (let brew of beer.brews) { %>
                          <tr class="text-center">
                            <td><%= brew.brewDate %></td>
                            <td><%= brew.recipeId %></td>
                            <td></td>
                            <td></td>
                            <td><a href="/beers/<%= beer.id %>/recipes/<%= brew.recipeId %>/brews/<%= brew.id %>" class="btn btn-sm btn-outline-dark mb-2 float-right" role="button"><i class="far fa-eye"></i></a></td>
                          </tr>
                        <% } %>          
                      </tbody>
                    </table>
                    <% } else { %>
                    <p class="text-lite">This beer hasn't been brewed yet.</p>
                    <% } %>
                  </div>
                </div>
              </div>
            </div>
            <% } %>
          </div>
        </div>
      

      <!-- jQuery CDN -->
      <script
      src="https://code.jquery.com/jquery-3.1.1.min.js"
      integrity="sha256-hVVnYaiADRTO2PzUGmuLJr8BLUSjGIZsDYGmIJLv2b8="
      crossorigin="anonymous"></script>

      <!-- Bootstrap and its dependencies -->
      <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js" integrity="sha384-ZMP7rVo3mIykV+2+9J3UJ46jBk0WLaUAdn689aCwoqbBJiSnjAK/l8WvCWPIPm49" crossorigin="anonymous"></script>
      <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js" integrity="sha384-ChfqqxuZUCnJSK3+MXmPNIyE6ZbWh2IMqE241rYiqJxyMiZ6OW/JmZQ5stwEULTy" crossorigin="anonymous"></script>

      <!-- Custom JS & jQuery for recipe form-->
      <!-- <script src="/js/forms.js" type="text/javascript"></script> -->
      <script src="/js/script.js" type="text/javascript"></script>
  </body>
</html>